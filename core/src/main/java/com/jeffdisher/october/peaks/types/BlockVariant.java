package com.jeffdisher.october.peaks.types;


/**
 * The enum used when looking up an block texture in a TextureAtlas:  Blocks use the same texture for all 4 sides but
 * have special top and bottom variants.
 */
public enum BlockVariant
{
	TOP,
	BOTTOM,
	SIDE,
}
